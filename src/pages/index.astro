---
import Layout from "../layouts/Layout.astro";
import Footer from "../components/Footer.astro";
import Socials from "../components/Socials.astro";
---

<Layout title="Mike's House presents...Nite Session + Open Decks - November 25, 2024 @ The Graryson Bar DTLA">
	<main>
		<img
			class="poster-text"
			src="https://mikeshouse.tv/assets/Nite%20session%20text.png"
			alt="transparent poster with text only for Nite Session event"
		/>
		<Footer />
		<Socials />
		<div class="poster-text">
			<h1>Mike's House presents Nite Session + Open Decks sponsored by Niteflyers</h1>
			<h2>Lineup</h2>
			<ul>
				<li>Avey</li>
				<li>Lex Looper</li>
				<li>Ketchup</li>
				<li>Mikezilla</li>
				<li>Juan Latino</li>
			</ul>
			<p>
				Nite Session is a new party presented by Mike’s House. It’s the same great community that brought you the Block Party Pop Up and Mike’s Day Out. This time we’re opening up the decks.
        <br>
        This is your opportunity to get valuable experience DJing for a crowd on professional equipment. Slots will fill up fast so get here early and stay late.
			</p>
		</div>
	</main>
</Layout>

<style>
  main {
    color: #ffffef;
    font-size: 20px;
  }
  div.poster-text {
    visibility: hidden;
  }
  .poster-text {
    max-height: 96vh;
    margin: 0 auto;
    display: block;
    margin-top: 0px;
    margin-bottom: -80px;
  }
  @media (max-width: 590px) {
    .poster-text {
      max-width: 435px;
      width: 100%;
      max-height: 96vh;
      display: block;
      margin-bottom: -75px;
    }
  }
  .astro-a {
    position: absolute;
    top: -32px;
    left: 50%;
    transform: translatex(-50%);
    width: 220px;
    height: auto;
    z-index: -1;
  }
  h1 {
    font-size: 4rem;
    font-weight: 700;
    line-height: 1;
    text-align: center;
    margin-bottom: 1em;
  }
  .text-gradient {
    background-image: var(--accent-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-size: 400%;
    background-position: 0%;
  }
  .instructions {
    margin-bottom: 2rem;
    border: 1px solid rgba(var(--accent-light), 25%);
    background: linear-gradient(
      rgba(var(--accent-dark), 66%),
      rgba(var(--accent-dark), 33%)
    );
    padding: 1.5rem;
    border-radius: 8px;
  }
  .instructions code {
    font-size: 0.8em;
    font-weight: bold;
    background: rgba(var(--accent-light), 12%);
    color: rgb(var(--accent-light));
    border-radius: 4px;
    padding: 0.3em 0.4em;
  }
  .instructions strong {
    color: rgb(var(--accent-light));
  }
  .link-card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(24ch, 1fr));
    gap: 2rem;
    padding: 0;
  }
</style>
